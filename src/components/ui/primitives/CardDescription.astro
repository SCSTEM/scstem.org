---
import type { HTMLAttributes } from "astro/types";

import { cn } from "@/lib/cn";

interface Props extends Omit<HTMLAttributes<"p">, "class"> {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<p class={cn("text-body", className)} {...rest}>
  <slot />
</p>
