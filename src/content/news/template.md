---
# Copy this file, rename it to a url-friendly slug (my-post-title.md), delete these comments,
# and set draft: false when it is ready. Leave this template file itself as draft: true.
title: A short, specific headline
date: 2026-01-01
description: One or two sentences. This is the meta description and the card summary, so write it for someone deciding whether to click.
# Optional. Put the image in src/assets/news/ first.
# heroImage: ../../assets/news/my-post.webp
draft: true
---

The post body, in markdown. `draft: true` keeps it out of the build — flip it to `false` to
publish.
