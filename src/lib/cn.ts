/**
 * The only sanctioned import site for `cn`. Components import it from `@/lib/cn`;
 * `clsx`, `classnames`, and `tailwind-merge` are banned repo-wide (see .oxlintrc.json).
 */
export { cn } from "cnfast";
