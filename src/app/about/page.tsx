import type { Metadata } from "next/types";

export const metadata: Metadata = {
  title: "About",
};

export default function About(): JSX.Element {
  return <main className="">hello about</main>;
}
