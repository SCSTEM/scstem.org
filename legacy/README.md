# legacy/

The Next.js 16 site that scstem.org ran on through 2026. **Reference only.**

- Do not import from here. Do not copy components. Do not build it.
- It is the source of truth for *content, copy, URLs, and behavior* while the Astro
  rewrite is in progress — read it, then rebuild the implementation.
- Excluded from every tool in the repo (TypeScript, oxlint, oxfmt, ESLint, Prettier, knip).
- Deleted at cutover (Phase 11).
